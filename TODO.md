# TODO List

- [x] Payment badge styling — icon + label with good contrast (implemented in `web/admin/styles.css`)
- [ ] Payment badge verification — in-progress

Notes:
- Styling was added to `.payment-badge` and related `.badge-*` classes in `web/admin/styles.css`.
- Follow-up: verify visual contrast across browsers and finish verification tests.
